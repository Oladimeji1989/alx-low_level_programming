#include <stdio.h>
/**
 * main - prints the lowercase alphabet in reverse
 * Return: 0 (success)
 */
int main(void)
{
	char A;

	A = 'z';
	while
		(A >= 'a')
		{
			putchar(A);
			A--;
		}
	putchar('\n');
	return (0);
}
