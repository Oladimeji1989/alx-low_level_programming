#include <stdio.h>
/**
 * main - Program that prints the alphabet in lowercase
 * Return: 0 (success)
 */
int main(void)
{
	char A;

	A = 'a';
	while
		(A <= 'z')
		{
			putchar(A);
				A++;
		}
		putchar('\n');
	return (0);
}
